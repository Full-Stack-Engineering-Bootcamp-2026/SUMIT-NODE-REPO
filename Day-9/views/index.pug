doctype html
html
  head
    title Products
  body
    h1 Product List

    a(href="/add-product") Add Product

    ul
      if products.length === 0
        li No products found
      else
        each p in products
          li #{p.title} - ₹#{p.price}